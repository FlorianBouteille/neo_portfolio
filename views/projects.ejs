<% layout('layout') %>

<ul>
  <% projects.forEach(project => { %>
    <li>
      <a href="/projects/<%= project.title %>" style="background-image: url('<%= project.image %>');">
        <h2><%= project.title %></h2>
      </a>
    </li>
  <% }); %>
</ul>
